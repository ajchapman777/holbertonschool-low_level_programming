#include "main.h"
/**
 * mul - function that checks for a digit (0 through).
 * @a: the a variable
 * @b: the b variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
